framework:
    rate_limiter:
        # Endpoints publics sensibles (webhook : secret dans l’URL, brute-force limité par IP)
        webhook_ingest:
            policy: 'sliding_window'
            limit: 120
            interval: '1 minute'
